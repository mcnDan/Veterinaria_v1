<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.veterinaria_v1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".registrarMascota"></activity>
        <activity android:name=".historialGeneralMasc" />
        <activity android:name=".fichaTecnica4" />
        <activity android:name=".fichaTecnica3" />
        <activity android:name=".fichaTecnica2" />
        <activity android:name=".fichaTecnica1" />
        <activity android:name=".listaMascotas" />
        <activity android:name=".registrarCliente" />
        <activity android:name=".buscarInicio" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>